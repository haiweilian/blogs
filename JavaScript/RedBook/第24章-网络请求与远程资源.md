---
title: 基础模板
date: 1990-01-01
updated: 1990-01-01
categories: 基础分类
---

## 基础标题

ajax 请求示例

```js
let xhr = new XMLHttpRequest();
xhr.onreadystatechange = function (event) {
  if (xhr.readyState === 4) {
    console.log(xhr.responseText);
  }
};
xhr.open("get", "exampl.txt", true);
xhr.send(null);
```

fetch 请求示例

```js
fetch("exampl.txt", {
  method: "GET", // 默认 GET
}).then((response) => {
  response.text().then((text) => {
    console.log(text);
  });
});
```

webSock 链接示例

```js
let socket = new WebSocket("ws://wwwww");
socket.onopen = function () {};
socket.onerror = function () {};
socket.onclose = function () {};
socket.onmessage = function (event) {};
socket.send({});
```
